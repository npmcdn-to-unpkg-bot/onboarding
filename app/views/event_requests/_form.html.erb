<%= form_for(event_request) do |f| %>
  <% if event_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_request.errors.count, "error") %> prohibited this event_request from being saved:</h2>

      <ul>
      <% event_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :potential_date %>
    <%= f.date_select :potential_date %>
  </div>

  <div class="field">
    <%= f.label :experience_level %>
    <%= f.text_field :experience_level %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
