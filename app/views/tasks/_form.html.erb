<%= form_for(task) do |f| %>
    <% if task.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

          <ul>
            <% task.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <fieldset class="form-group row">
      <%= f.label :name, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.text_field :name, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :task_manager_url, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.text_field :task_manager_url, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :type, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.text_field :type, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :description, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10">
        <%= f.text_area :description %>
      </div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :image, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.file_field :image, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :status, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.text_field :status, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :location, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.text_field :location, class: 'form-control' %></div>
    </fieldset>

    <fieldset class="form-group row">
      <%= f.label :campaign_ids, class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-10"><%= f.collection_select :campaign_ids, Campaign.all, :id, :name, {}, {:multiple => true, :class => 'form-control'} %>
      </div>
    </fieldset>

    <div class="form-group row">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit class: 'btn btn-primary' %>
        <%= link_to 'Show', task, class: 'btn btn-secondary' unless task.id.nil? %>
        <%= link_to 'Back', tasks_path, class: 'btn btn-secondary' %>
      </div>
    </div>
<% end %>
